<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <title>Dashboard</title>
</head>
<body>
    <a href="/browse">Browse</a>
    <a href="/musicplayer">MusicPlayer</a>
    <a href="/uploadmusic">Upload Music</a>
    <a href="/account">Account</a>
    <a href="/">Logout</a>
    <h1 class="text-color">Hello <%= user.charAt(0).toUpperCase() + user.slice(1) %></h1>
    <h2 class="text-color"><%= user.charAt(0).toUpperCase() + user.slice(1) %> Library</h2>
        <% for(var i = 0; i < pcount; i++) { 
            var sname = ''
            var sink = ''
            %>
            <div class="big-container">
            <table class="tablecontainer"> 
            <th class="playlist-header">
                  <%= playlist[i].playlist_name %> <button>+</button>
                    <tr>
                        <% test[i].forEach(d=> {
                            if (d.split("://")[1] != undefined)
                               sink = d.split("://")[1]
                            if (d.split('https')[0] != ' ')
                               sname = d.split('https')[0]
                            if(sink != undefined) {
                                 %>
                            <td class="text-color"><a href='<%= "https://".concat(sink) %>' target="blank"><%= sname %></a></td>

                            <% } %>
                        </tr>
                  </th>
                          
           <% }); %>
        </tr>
  </th>
        <% } %>
            </table>
        </div>
</body>
</html>